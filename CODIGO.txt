import React from 'react';
import { Trophy, TrendingUp, Unlock, Lightbulb, Terminal, Code, BrainCircuit, ArrowUpRight, ArrowDownRight, Minus, BookMarked, Mail, GraduationCap, Info, ExternalLink, Download } from 'lucide-react';

const Card = ({ title, icon: Icon, children, className = '' }) => (
  <div className={`bg-white border border-slate-200 rounded-lg p-5 shadow-sm ${className}`}>
    <div className="flex items-center gap-2 mb-4 border-b border-slate-100 pb-3">
      <Icon className="w-5 h-5 text-indigo-600" />
      <h3 className="text-lg font-bold text-slate-800 tracking-wide">{title}</h3>
    </div>
    <div className="text-slate-600">
      {children}
    </div>
  </div>
);

const TrendBadge = ({ text, type }) => {
  const styles = {
    up: "bg-emerald-100 text-emerald-700 border-emerald-200",
    down: "bg-rose-100 text-rose-700 border-rose-200",
    neutral: "bg-slate-100 text-slate-600 border-slate-200"
  };
  return (
    <span className={`px-2 py-1 rounded text-xs border font-mono ${styles[type]}`}>
      {text}
    </span>
  );
};

export default function ArenaLeaderboardMonitor() {
  const top5Data = [
    { rank: 1, name: 'Claude Opus 4.6 Thinking', elo: 1505, ci: '±6', type: 'Propiedad (Cerrado)', org: 'Anthropic', trend: 'up' },
    { rank: 2, name: 'Gemini 3.1 Ultra', elo: 1504, ci: '±6', type: 'Propiedad (Cerrado)', org: 'Google', trend: 'up' },
    { rank: 3, name: 'Claude Opus 4.6', elo: 1499, ci: '±6', type: 'Propiedad (Cerrado)', org: 'Anthropic', trend: 'down' },
    { rank: 4, name: 'Grok 4.20 Beta 1', elo: 1494, ci: '±7', type: 'Propiedad (Cerrado)', org: 'xAI', trend: 'neutral' },
    { rank: 5, name: 'Llama-4 400B Instruct', elo: 1489, ci: '±7', type: 'Abierto (Llama License)', org: 'Meta', trend: 'up' },
  ];

  // Datos históricos simulados para los últimos 30 días (4 semanas)
  const historyData = [
    { name: 'Claude Opus 4.6 Thinking', color: '#4f46e5', data: [1490, 1495, 1502, 1505] }, // Indigo 600
    { name: 'Gemini 3.1 Ultra', color: '#0ea5e9', data: [1485, 1490, 1495, 1504] },       // Sky 500
    { name: 'Llama-4 400B Instruct', color: '#10b981', data: [1450, 1460, 1475, 1489] }   // Emerald 500
  ];

  const handleDownload = () => {
    const headers = ['Rank,Modelo,Organización,Elo,Intervalo,Licencia'];
    const rows = top5Data.map(d => `${d.rank},"${d.name}","${d.org}",${d.elo},"${d.ci}","${d.type}"`);
    const csvContent = "data:text/csv;charset=utf-8," + headers.concat(rows).join("\n");
    const encodedUri = encodeURI(csvContent);
    const link = document.createElement("a");
    link.setAttribute("href", encodedUri);
    link.setAttribute("download", "arena_elo_data_mar30_2026.csv");
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  };

  // Helper para generar el path SVG del gráfico
  const generatePath = (data, minElo = 1440, maxElo = 1510) => {
    const range = maxElo - minElo;
    return data.map((val, i) => {
      const x = (i / (data.length - 1)) * 400; 
      const y = 100 - ((val - minElo) / range) * 100;
      return `${i === 0 ? 'M' : 'L'} ${x} ${y}`;
    }).join(' ');
  };

  return (
    <div className="min-h-screen bg-slate-50 text-slate-800 p-6 font-sans selection:bg-indigo-100 selection:text-indigo-900">
      
      {/* Header */}
      <header className="mb-6 flex flex-col md:flex-row justify-between items-start md:items-end border-b border-slate-200 pb-4 gap-4">
        <div>
          <h1 className="text-3xl font-extrabold text-slate-900 tracking-tighter flex items-center gap-3">
            <Terminal className="w-8 h-8 text-indigo-600" />
            LMSYS ARENA SCOUT
          </h1>
          <p className="text-slate-500 text-sm mt-1 font-mono">
            Filtrando el ruido de la carrera IA | Corte de datos: 30 de Marzo, 2026
          </p>
        </div>
        <div className="flex flex-col md:flex-row items-end gap-3 text-right">
          <button 
            onClick={handleDownload}
            className="inline-flex items-center gap-2 px-4 py-1.5 bg-white border border-slate-300 rounded-lg font-bold text-sm text-slate-700 shadow-sm hover:bg-slate-50 hover:text-indigo-600 transition-colors focus:ring-2 focus:ring-indigo-500 focus:outline-none"
            title="Descargar conjunto de datos en formato CSV"
          >
            <Download className="w-4 h-4" />
            Exportar CSV
          </button>
          <div className="inline-flex items-center gap-2 px-3 py-1.5 bg-indigo-50 border border-indigo-200 rounded-full font-mono text-xs text-indigo-700 font-semibold shadow-sm">
            <span className="relative flex h-2.5 w-2.5">
              <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-indigo-400 opacity-75"></span>
              <span className="relative inline-flex rounded-full h-2.5 w-2.5 bg-indigo-600"></span>
            </span>
            SYNC: CHATBOT ARENA LIVE
          </div>
        </div>
      </header>

      {/* RECUADRO INTRODUCTORIO */}
      <div className="bg-indigo-50 border border-indigo-100 rounded-lg p-5 mb-6 flex gap-4 items-start shadow-sm">
        <div className="bg-indigo-100 p-2 rounded-full mt-0.5 flex-shrink-0">
          <Info className="w-5 h-5 text-indigo-600" />
        </div>
        <div>
          <h3 className="text-indigo-900 font-bold mb-1">Sobre este Reporte</h3>
          <p className="text-sm text-indigo-800/80 leading-relaxed">
            Este panel resume el estado del arte de la Inteligencia Artificial utilizando datos del <strong>LMSYS Chatbot Arena</strong>. En lugar de depender de benchmarks estáticos que los modelos memorizan, este reporte se basa en pruebas a ciegas (crowdsourcing) donde humanos reales eligen qué modelo da la mejor respuesta. Los resultados se calculan mediante el <strong>sistema de puntuación Elo</strong> (similar al ajedrez), filtrando el ruido comercial para mostrar quién está ganando realmente la carrera de la IA hoy.
          </p>
        </div>
      </div>

      {/* 1. EL TOP 5 ACTUAL */}
      <Card title="🏆 El Top 5 Actual (Overall)" icon={Trophy} className="mb-6 border-t-4 border-t-indigo-600">
        <div className="overflow-x-auto">
          <table className="w-full text-left text-sm font-mono">
            <thead className="text-xs uppercase bg-slate-100 text-slate-600">
              <tr>
                <th className="px-4 py-3 rounded-tl-lg">Rank</th>
                <th className="px-4 py-3">Modelo</th>
                <th className="px-4 py-3">Organización</th>
                <th className="px-4 py-3">Elo Score (95% CI)</th>
                <th className="px-4 py-3 rounded-tr-lg">Licencia</th>
              </tr>
            </thead>
            <tbody>
              {top5Data.map((model) => (
                <tr key={model.rank} className="border-b border-slate-100 hover:bg-slate-50 transition-colors">
                  <td className="px-4 py-4 font-bold text-slate-500">#{model.rank}</td>
                  <td className="px-4 py-4 font-bold text-indigo-700 flex items-center gap-2">
                    {model.name}
                    {model.trend === 'up' && <ArrowUpRight className="w-4 h-4 text-emerald-600" title="Subió posiciones" />}
                    {model.trend === 'down' && <ArrowDownRight className="w-4 h-4 text-rose-600" title="Bajó posiciones" />}
                    {model.trend === 'neutral' && <Minus className="w-4 h-4 text-slate-400" title="Sin cambios" />}
                  </td>
                  <td className="px-4 py-4 text-slate-600 font-semibold">{model.org}</td>
                  <td className="px-4 py-4">
                    <span className="text-lg font-extrabold text-slate-800 mr-1">{model.elo}</span>
                    <span className="text-xs text-slate-500">{model.ci}</span>
                  </td>
                  <td className="px-4 py-4">
                    <span className={`px-2 py-1 text-[10px] uppercase tracking-wider rounded font-bold border ${model.type.includes('Abierto') ? 'bg-emerald-50 text-emerald-700 border-emerald-200' : 'bg-rose-50 text-rose-700 border-rose-200'}`}>
                      {model.type}
                    </span>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </Card>

      {/* EVOLUCIÓN 30 DÍAS */}
      <Card title="📊 Evolución a 30 Días (Top 3 Modelos Disruptivos)" icon={TrendingUp} className="mb-6 border-l-4 border-l-indigo-400">
        <div className="flex flex-col md:flex-row gap-6 items-center">
          <div className="w-full md:w-2/3 relative h-40 bg-slate-50 border border-slate-200 rounded-lg p-4">
            {/* Eje Y (Etiquetas) */}
            <div className="absolute left-2 top-2 bottom-6 flex flex-col justify-between text-[10px] text-slate-400 font-mono">
              <span>1510</span>
              <span>1475</span>
              <span>1440</span>
            </div>
            
            {/* Gráfico SVG */}
            <svg viewBox="0 0 400 100" className="w-full h-full ml-6 overflow-visible">
              {/* Líneas de cuadrícula horizontales */}
              <line x1="0" y1="0" x2="400" y2="0" stroke="#e2e8f0" strokeDasharray="4 4" />
              <line x1="0" y1="50" x2="400" y2="50" stroke="#e2e8f0" strokeDasharray="4 4" />
              <line x1="0" y1="100" x2="400" y2="100" stroke="#e2e8f0" strokeDasharray="4 4" />

              {/* Trazo de líneas de datos */}
              {historyData.map((series, idx) => (
                <g key={idx}>
                  <path 
                    d={generatePath(series.data)} 
                    fill="none" 
                    stroke={series.color} 
                    strokeWidth="3" 
                    strokeLinecap="round" 
                    strokeLinejoin="round" 
                    className="drop-shadow-md transition-all duration-500 hover:stroke-[4px]"
                  />
                  {/* Puntos de datos */}
                  {series.data.map((val, i) => {
                    const cx = (i / (series.data.length - 1)) * 400;
                    const cy = 100 - ((val - 1440) / 70) * 100;
                    return (
                      <circle key={i} cx={cx} cy={cy} r="4" fill="white" stroke={series.color} strokeWidth="2" />
                    );
                  })}
                </g>
              ))}
            </svg>
            
            {/* Eje X (Etiquetas) */}
            <div className="absolute bottom-1 left-10 right-4 flex justify-between text-[10px] text-slate-500 font-bold uppercase tracking-wider">
              <span>Semana -3</span>
              <span>Semana -2</span>
              <span>Semana -1</span>
              <span>Hoy</span>
            </div>
          </div>
          
          <div className="w-full md:w-1/3">
            <h4 className="text-sm font-bold text-slate-700 mb-3 border-b border-slate-200 pb-2">Leyenda & Análisis</h4>
            <ul className="space-y-3">
              {historyData.map((series, idx) => (
                <li key={idx} className="flex items-start gap-2 text-xs text-slate-600">
                  <div className="w-3 h-3 rounded-full mt-0.5 flex-shrink-0 shadow-sm" style={{ backgroundColor: series.color }}></div>
                  <div>
                    <strong className="block text-slate-800">{series.name}</strong>
                    <span className="text-[10px] text-slate-500 font-mono">Δ {series.data[3] - series.data[0]} pts (30d)</span>
                  </div>
                </li>
              ))}
            </ul>
            <p className="mt-4 text-xs text-slate-500 leading-relaxed bg-indigo-50/50 p-2 rounded border border-indigo-100/50">
              *Se observa la convergencia asintótica de los modelos propietarios (Claude/Gemini) frente al crecimiento exponencial de parámetros abiertos (Llama-4).
            </p>
          </div>
        </div>
      </Card>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
        
        {/* 2. MOVIMIENTOS SÍSMICOS */}
        <Card title="📈 Movimientos Sísmicos" icon={TrendingUp}>
          <div className="space-y-4">
            <div className="bg-slate-50 p-4 rounded border border-slate-200 border-l-4 border-l-indigo-500 shadow-sm">
              <h4 className="text-sm font-bold text-slate-800 mb-1">Colisión en la Cúspide: Claude vs Gemini Ultra</h4>
              <p className="text-xs text-slate-600 leading-relaxed">
                El lanzamiento de <strong className="text-slate-900">Gemini 3.1 Ultra</strong> ha inyectado volatilidad en la cima. Con 1504 puntos Elo, ha cerrado la brecha frente a Claude Opus 4.6 Thinking (1505). Evaluando la matriz de Bradley-Terry y el intervalo de confianza (±6), confirmamos que estadísticamente son indistinguibles. La preferencia humana oscila marginalmente dependiendo de la longitud del contexto.
              </p>
            </div>
            
            <div className="bg-slate-50 p-4 rounded border border-slate-200 border-l-4 border-l-emerald-500 shadow-sm">
              <h4 className="text-sm font-bold text-slate-800 mb-1">Exclusión Histórica de OpenAI</h4>
              <p className="text-xs text-slate-600 leading-relaxed">
                Por primera vez desde la concepción del Arena, OpenAI no tiene un modelo en el Top 5. <strong className="text-slate-900">GPT-5.4 High</strong> fue desplazado al 6to lugar (1486 Elo) tras la fuerte entrada de Llama-4 400B. La fricción se nota especialmente en su tasa de rechazo por *Safety Guardrails*, la cual los usuarios penalizan severamente.
              </p>
            </div>
          </div>
        </Card>

        {/* 3. EL LÍDER OPEN SOURCE */}
        <Card title="🔓 El Líder Open Source" icon={Unlock}>
          <div className="flex flex-col h-full justify-between">
            <div>
              <div className="flex justify-between items-start mb-4">
                <div>
                  <h4 className="text-xl font-extrabold text-emerald-700">Llama-4 400B Instruct</h4>
                  <p className="text-xs text-slate-500 uppercase tracking-widest mt-1 font-semibold">Meta (Llama License)</p>
                </div>
                <div className="text-right">
                  <span className="text-3xl font-mono font-black text-slate-800 block">1489</span>
                  <span className="text-xs text-slate-500 font-bold uppercase tracking-wider">Elo Score</span>
                </div>
              </div>
              <p className="text-sm text-slate-600 leading-relaxed mb-4">
                Desplazamiento radical: Meta recupera la corona abierta frente a los modelos chinos (Qwen/GLM). Llama-4 entra directamente al Top 5 global, demostrando que la destilación de arquitecturas masivas a pesos descargables está acortando el ciclo de *catch-up* exponencialmente.
              </p>
            </div>
            
            <div className="bg-slate-50 p-3 rounded border border-slate-200 text-xs shadow-inner">
              <div className="flex justify-between items-center mb-2">
                <span className="text-slate-600 font-semibold">Brecha con la Frontera (Propietario):</span>
                <span className="font-mono text-emerald-600 font-bold">Apenas -16 Puntos Elo</span>
              </div>
              {/* Barra de progreso invertida visualizando la brecha */}
              <div className="w-full bg-slate-200 rounded-full h-2 overflow-hidden">
                <div className="bg-gradient-to-r from-emerald-400 to-emerald-600 h-2" style={{ width: '97%' }}></div>
              </div>
              <p className="text-[10.5px] text-slate-500 mt-2 text-center leading-tight">
                *La distancia se desplomó de 50 a 16 puntos. El rendimiento empírico de Llama-4 (1489) se superpone con el límite inferior del intervalo de confianza de Gemini 3.1 Pro Preview (1494).
              </p>
            </div>
          </div>
        </Card>

      </div>

      {/* 4. ANÁLISIS DEL EXPERTO (Hard Prompts & Coding) */}
      <Card title="💡 Análisis del Experto: Categorías Complejas" icon={BrainCircuit} className="mb-8 bg-indigo-50/30">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div className="bg-white p-4 rounded-lg border border-slate-200 shadow-sm">
            <h4 className="text-sm font-bold text-indigo-700 flex items-center gap-2 mb-2 pb-2 border-b border-slate-100">
              <Code className="w-4 h-4" /> Categoría: Coding (1520+ Elo)
            </h4>
            <p className="text-sm text-slate-600 leading-relaxed">
              Aunque Claude Opus 4.6 Thinking retiene el liderazgo indiscutible en arquitecturas multi-agente, <strong className="text-slate-900">Llama-4 400B</strong> está canibalizando la cuota de preferencia en tareas de *scripting* estándar (Python/React). La comunidad matemática e ingenieril aprecia su latencia reducida y su negativa nula a refactorizar código sin sermones morales.
            </p>
          </div>
          <div className="bg-white p-4 rounded-lg border border-slate-200 shadow-sm">
            <h4 className="text-sm font-bold text-indigo-700 flex items-center gap-2 mb-2 pb-2 border-b border-slate-100">
              <Lightbulb className="w-4 h-4" /> Categoría: Hard Prompts
            </h4>
            <p className="text-sm text-slate-600 leading-relaxed">
              En la resolución de teoremas y deducción lógica multi-salto, <strong className="text-slate-900">Gemini 3.1 Ultra</strong> ha marcado un hito. La vectorización de su contexto nativo multimodal (capaz de digerir *papers* enteros en PDF sin perder recuperación de hechos) le otorga una ventaja competitiva en esta categoría que sobrepasa el error estocástico del *crowdsourcing*.
            </p>
          </div>
        </div>
      </Card>

      {/* FUENTES Y REFERENCIAS */}
      <div className="mb-8">
        <h4 className="text-sm font-bold text-slate-700 uppercase tracking-wider mb-3 flex items-center gap-2">
          <BookMarked className="w-4 h-4 text-slate-500" />
          Fuentes y Metodología
        </h4>
        <div className="bg-white border border-slate-200 rounded-lg p-4 shadow-sm flex flex-col md:flex-row gap-4 md:gap-8">
          <div className="flex-1">
            <a href="https://lmarena.ai/?leaderboard" target="_blank" rel="noopener noreferrer" className="text-indigo-600 font-semibold hover:text-indigo-800 flex items-center gap-1 mb-1">
              LMSYS Chatbot Arena Leaderboard <ExternalLink className="w-3 h-3" />
            </a>
            <p className="text-xs text-slate-500">
              Datos extraídos de la plataforma oficial Large Model Systems Organization (UC Berkeley, UCSD, CMU). Las puntuaciones Elo se basan en el modelo Bradley-Terry para estimar la probabilidad de victoria en batallas pareadas.
            </p>
          </div>
          <div className="flex-1">
            <h5 className="text-slate-700 font-semibold mb-1 text-sm">Categorías de Evaluación</h5>
            <p className="text-xs text-slate-500">
              El análisis se focalizó en los subsets de <em>Overall</em> (General), <em>Coding</em> (Programación) y <em>Hard Prompts</em> (Razonamiento Complejo), filtrando modelos con menos de 10,000 votos para garantizar significancia estadística.
            </p>
          </div>
        </div>
      </div>

      {/* CRÉDITOS Y FIRMA */}
      <div className="border-t border-slate-200 pt-6">
        <div className="bg-white border border-slate-200 rounded-lg p-5 flex flex-col md:flex-row items-center justify-between gap-4 shadow-sm">
          <div className="flex items-center gap-4">
            <div className="bg-indigo-50 p-3 rounded-full border border-indigo-100">
              <GraduationCap className="w-6 h-6 text-indigo-600" />
            </div>
            <div>
              <h3 className="text-slate-900 font-extrabold text-lg tracking-wide">Andrés Mauricio Grisales A.</h3>
              <p className="text-indigo-600 text-sm font-mono mt-0.5 font-semibold">PhD Ciencias Matemáticas con énfasis en NLP</p>
            </div>
          </div>
          
          <div className="flex items-center gap-2 text-slate-600 text-sm font-mono bg-slate-50 px-4 py-2 rounded-full border border-slate-200 hover:text-indigo-700 hover:border-indigo-300 transition-colors cursor-pointer shadow-sm">
            <Mail className="w-4 h-4" />
            <a href="mailto:andresmgrisales@gmail.com" className="font-semibold">andresmgrisales@gmail.com</a>
          </div>
        </div>
      </div>

    </div>
  );
}